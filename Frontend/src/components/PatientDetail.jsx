import { useState } from "react";
import axios from "axios"

export const PatientDetail = () => {

    

    // async function getPatient(){
    //         let result = await axios.get(`http://localhost:5000/api/user/view/${id}`)
    //         setData(result.data)
    // }

    return (
        <>
         <h1>Add Doctor</h1>
        </>
        
    )
}